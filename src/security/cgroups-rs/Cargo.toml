[package]
name = "cgroups-rs"
version = "3.1.0"
edition = "2021"

[dependencies]
colored = "2.0"
libc = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
log = "0.4"

# Optional systemd integration via D-Bus
# zbus = { version = "4", optional = true }

# [build-dependencies]
# cc = "1.0"

[features]
default = []
systemd = []  # Enable zbus-based systemd integration
