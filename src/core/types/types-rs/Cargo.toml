[package]
name = "types-rs"
version = "3.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
libc = { workspace = true }

[dev-dependencies]
serde_json = "1.0"

[build-dependencies]
cbindgen = { workspace = true }